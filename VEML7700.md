# VEML7700 驱动与数据链路

## 1. 模块定位

VEML7700 是环境光传感器，通过 I2C 接入 Linux，内核以 IIO 设备提供 `in_illuminance_input`. Qt 用户态负责识别设备、读取光照值和报告在线状态.

## 2. 设备发现

默认 IIO 根目录是 `/sys/bus/iio/devices`. `discoverVeml7700Path()` 遍历 `iio:device*`，读取每个目录的 `name`，不区分大小写匹配 `veml7700`. 也可用 `ENVIRONMENT_MONITOR_VEML7700_SYSFS` 直接指定设备目录或输入文件路径.

构造函数保存发现结果到 `veml7700Path_`，并用路径是否为空初始化 `veml7700Online_`. `start()` 启动定时采样，`setEnabledDevices()` 通过位掩码启用或禁用 VEML7700.

## 3. 调用链

```text
QTimer::timeout
  -> SimulatedSensorProvider::sample
  -> discoverVeml7700Path
  -> readVeml7700
  -> SensorSnapshot.illuminance
  -> snapshotReady
  -> MainWindow 曲线、告警和 CSV
  -> Esp8266Controller::publishTelemetry
```

状态名称为 `VEML7700 / I2C`，状态文本可以区分禁用、等待、IIO 不可用、采集中和读取失败.

## 4. 调试和限制

```bash
find /sys/bus/iio/devices -maxdepth 2 -name name -exec sh -c 'printf "%s: " "$1"; cat "$1"' _ {} \\\;
cat /sys/bus/iio/devices/iio:deviceX/in_illuminance_input
```

当前用户态不设置 VEML7700 增益、积分时间或 I2C 地址，这些配置由内核驱动和设备树负责. 若亮度单位或范围不符合预期，应先检查内核 IIO 属性和传感器配置，不要直接在 `sample()` 中加入未经确认的倍率.

## 5. `sensorprovider.cpp` 逐行解析

### 5.1 初始化

```cpp
veml7700Path_(discoverVeml7700Path()),
veml7700Online_(!veml7700Path_.isEmpty())
```

第一行在构造函数初始化列表中寻找 VEML7700 目录并保存路径, 第二行把“路径非空”转成初始在线标记. 它只代表发现成功, 不能替代后续文件读取校验.

```cpp
const QString configuredPath = qEnvironmentVariable("ENVIRONMENT_MONITOR_VEML7700_SYSFS").trimmed();
if (!configuredPath.isEmpty())
    return QFile::exists(configuredPath) ? configuredPath : QString();
```

先读取覆盖环境变量并去空白. 配置存在时立即检查路径, 存在则返回, 不存在则返回空字符串, 不会悄悄改用另一个目录.

```cpp
const QString iioRoot = qEnvironmentVariable("ENVIRONMENT_MONITOR_IIO_ROOT",
                                              QStringLiteral("/sys/bus/iio/devices"));
const QDir root(iioRoot);
const QStringList devices = root.entryList(QStringList() << QStringLiteral("iio:device*"),
                                            QDir::Dirs | QDir::NoDotAndDotDot, QDir::Name);
```

未指定覆盖路径时使用标准 IIO 根目录. `entryList` 只枚举设备目录, 排除点目录, 并按名称排序, 使扫描结果稳定.

```cpp
for (const QString &device : devices) {
    QFile nameFile(root.filePath(device + QStringLiteral("/name")));
    if (!nameFile.open(QIODevice::ReadOnly | QIODevice::Text))
        continue;
    if (QString::fromLocal8Bit(nameFile.readAll()).trimmed().compare(
            QStringLiteral("veml7700"), Qt::CaseInsensitive) == 0)
        return root.filePath(device);
}
return QString();
```

循环为每个设备打开 `name`. 文件打不开就跳过. 读取结果去掉换行后和 `veml7700` 比较, 匹配时返回设备目录, 循环结束返回空路径表示未发现.

### 5.2 读取光照值

```cpp
const QString inputPath = veml7700Path_.endsWith(QStringLiteral("_input"))
    ? veml7700Path_ : veml7700Path_ + QStringLiteral("/in_illuminance_input");
QFile inputFile(inputPath);
if (!inputFile.open(QIODevice::ReadOnly | QIODevice::Text)) {
    if (errorMessage) *errorMessage = inputFile.errorString();
    return false;
}
bool ok = false;
*illuminance = QString::fromLocal8Bit(inputFile.readAll()).trimmed().toDouble(&ok);
if (!ok || !qIsFinite(*illuminance)) {
    if (errorMessage) *errorMessage = QStringLiteral("VEML7700 光照值无效");
    return false;
}
return true;
```

- 第一行兼容两种输入: 环境变量可以指向设备目录, 也可以直接指向 `_input` 文件.
- `QFile` 打开失败时把系统错误写回调用者.
- `toDouble(&ok)` 转换 sysfs 文本, `qIsFinite` 拒绝 NaN 和无穷大.
- 成功时通过输出指针写入光照值, 返回 true.

### 5.3 `sample()` 中的错误状态

```cpp
if (veml7700Path_.isEmpty())
    veml7700Path_ = discoverVeml7700Path();
QString errorMessage;
if (veml7700Path_.isEmpty() || !readVeml7700(&snapshot.illuminance, &errorMessage)) {
    snapshot.illuminance = qQNaN();
    if (veml7700Online_) {
        veml7700Online_ = false;
        if (!errorMessage.isEmpty())
            emit providerError(QStringLiteral("VEML7700 IIO 读取失败: %1").arg(errorMessage));
        updateDeviceStatuses();
    }
} else if (!veml7700Online_) {
    veml7700Online_ = true;
    updateDeviceStatuses();
}
```

先在运行期补做设备发现, 再读取. 失败时写入 `NaN` 并只在首次转离线时报告错误, 成功恢复时重新广播设备状态. 这种边沿触发避免每次定时采样都重复发送相同错误.
## 6. Linux 内核里的 VEML7700

真正访问 VEML7700 的代码是:

`drivers/iio/light/veml7700.c`

```c
/*
 * Vishay VEML7700 ambient light sensor driver.
 *
 * The default configuration is gain x1 and 100 ms integration time,
 * yielding 0.0576 lux per ALS count.
 */
#include <linux/delay.h>
#include <linux/i2c.h>
#include <linux/module.h>
#include <linux/mutex.h>
#include <linux/of.h>
#include <linux/iio/iio.h>

#define VEML7700_REG_ALS_CONF 0x00
#define VEML7700_REG_ALS_DATA 0x04

struct veml7700_data {
	struct i2c_client *client;
	struct mutex lock;
};

static int veml7700_write_word(struct veml7700_data *data,
			       unsigned int reg, u16 value)
{
	u8 buf[3] = { reg, value & 0xff, value >> 8 };
	int ret;

	ret = i2c_master_send(data->client, buf, sizeof(buf));
	return ret == sizeof(buf) ? 0 : (ret < 0 ? ret : -EIO);
}

static int veml7700_read_word(struct veml7700_data *data,
			      unsigned int reg, u16 *value)
{
	struct i2c_msg msgs[2];
	u8 command = reg;
	u8 buf[2];
	int ret;

	msgs[0].addr = data->client->addr;
	msgs[0].flags = 0;
	msgs[0].len = 1;
	msgs[0].buf = &command;
	msgs[1].addr = data->client->addr;
	msgs[1].flags = I2C_M_RD;
	msgs[1].len = sizeof(buf);
	msgs[1].buf = buf;

	ret = i2c_transfer(data->client->adapter, msgs, ARRAY_SIZE(msgs));
	if (ret != ARRAY_SIZE(msgs))
		return ret < 0 ? ret : -EIO;

	*value = buf[0] | ((u16)buf[1] << 8);
	return 0;
}

static int veml7700_read_raw(struct iio_dev *indio_dev,
			     struct iio_chan_spec const *chan,
			     int *val, int *val2, long mask)
{
	struct veml7700_data *data = iio_priv(indio_dev);
	u16 raw;
	u32 micro_lux;
	int ret;

	if (chan->type != IIO_LIGHT || mask != IIO_CHAN_INFO_PROCESSED)
		return -EINVAL;

	mutex_lock(&data->lock);
	ret = veml7700_read_word(data, VEML7700_REG_ALS_DATA, &raw);
	mutex_unlock(&data->lock);
	if (ret < 0)
		return ret;

	/* Default gain x1, integration time 100 ms: 0.0576 lux/count. */
	micro_lux = (u32)raw * 57600;
	*val = micro_lux / 1000000;
	*val2 = micro_lux % 1000000;
	return IIO_VAL_INT_PLUS_MICRO;
}

static const struct iio_info veml7700_info = {
	.driver_module = THIS_MODULE,
	.read_raw = veml7700_read_raw,
};

static const struct iio_chan_spec veml7700_channels[] = {
	{
		.type = IIO_LIGHT,
		.info_mask_separate = BIT(IIO_CHAN_INFO_PROCESSED),
	},
};

static int veml7700_probe(struct i2c_client *client,
			  const struct i2c_device_id *id)
{
	struct iio_dev *indio_dev;
	struct veml7700_data *data;
	int ret;

	indio_dev = devm_iio_device_alloc(&client->dev, sizeof(*data));
	if (!indio_dev)
		return -ENOMEM;

	data = iio_priv(indio_dev);
	data->client = client;
	mutex_init(&data->lock);

	/* Power on, gain x1, 100 ms integration time, interrupts disabled. */
	ret = veml7700_write_word(data, VEML7700_REG_ALS_CONF, 0x0000);
	if (ret < 0)
		return ret;
	msleep(100);

	indio_dev->name = id->name;
	indio_dev->dev.parent = &client->dev;
	indio_dev->info = &veml7700_info;
	indio_dev->channels = veml7700_channels;
	indio_dev->num_channels = ARRAY_SIZE(veml7700_channels);
	indio_dev->modes = INDIO_DIRECT_MODE;

	return devm_iio_device_register(&client->dev, indio_dev);
}

static const struct of_device_id veml7700_of_match[] = {
	{ .compatible = "vishay,veml7700" },
	{ },
};
MODULE_DEVICE_TABLE(of, veml7700_of_match);

static const struct i2c_device_id veml7700_id[] = {
	{ "veml7700", 0 },
	{ },
};
MODULE_DEVICE_TABLE(i2c, veml7700_id);

static struct i2c_driver veml7700_driver = {
	.driver = {
		.name = "veml7700",
		.of_match_table = of_match_ptr(veml7700_of_match),
	},
	.probe = veml7700_probe,
	.id_table = veml7700_id,
};
module_i2c_driver(veml7700_driver);

MODULE_AUTHOR("Keke Ming <ming.jvle@gmail.com>");
MODULE_DESCRIPTION("Vishay VEML7700 ambient light sensor driver");
MODULE_LICENSE("GPL v2");
```

这个驱动先定义两个寄存器: `0x00` 用来配置传感器, `0x04` 用来读取光照计数。`veml7700_data` 保存 I2C 客户端和互斥锁, 保证同一时间只有一个线程访问 I2C。

写寄存器时, 驱动把寄存器号和 16 位数据拆成低字节、高字节:

```c
u8 buf[3] = { reg, value & 0xff, value >> 8 };
ret = i2c_master_send(data->client, buf, sizeof(buf));
return ret == sizeof(buf) ? 0 : (ret < 0 ? ret : -EIO);
```

3 个字节全部发出去才算成功。总线返回负数时保留原错误, 只发出一部分时改成 `-EIO`。

读寄存器分两步完成: 第一条 I2C 消息发送寄存器号, 第二条消息带 `I2C_M_RD` 读取 2 个字节。驱动要求 `i2c_transfer()` 返回 2, 否则认为读失败。成功后用 `buf[0] | (buf[1] << 8)` 拼出传感器的 16 位计数。

IIO 的 `veml7700_read_raw()` 只接受光照通道和 processed 属性。它先加锁读 `0x04`, 读完立即解锁。默认配置下每个计数代表 `0.0576 lux`, 所以代码做这个换算:

```c
micro_lux = (u32)raw * 57600;
*val = micro_lux / 1000000;
*val2 = micro_lux % 1000000;
return IIO_VAL_INT_PLUS_MICRO;
```

IIO 用 `val` 表示整数部分, `val2` 表示小数的微单位, 所以用户空间读到的是已经换算好的 lux, Qt 不需要知道 `57600` 这个系数。

`veml7700_probe()` 被内核调用时, 它分配 IIO 设备, 保存 I2C client, 初始化锁, 向配置寄存器写 `0x0000`, 然后等待 100 ms 让一次积分完成。之后填入光照 channel 和 `read_raw` 回调, 调用 `devm_iio_device_register()` 创建 sysfs 节点。

设备树节点是:

```dts
veml7700@10 {
    compatible = "vishay,veml7700";
    reg = <0x10>;
};
```

`compatible` 匹配驱动的 OF 表, `0x10` 是硬件 I2C 地址。内核注册后, Qt 通过 `name` 文件找到 `veml7700`, 再读取 `in_illuminance_input`。