# RS485 温湿度模块与数据链路

## 1. 模块定位

RS485 在本项目中代表外接温湿度计，硬件接口规划为 UART3 的 TX/RX. 当前代码尚未实现真实串口打开、Modbus RTU 帧组装、CRC 校验或寄存器解析；采样层使用确定性的正弦曲线模拟湿度，用于验证界面、告警、CSV 和遥测流程.

因此本文中的“RS485 驱动”是应用层数据占位实现，不是已完成的硬件驱动.

## 2. 当前代码行为

`SensorDevice` 中 `SensorRs485 = 2`，和 BMP580、VEML7700 一起组成 `SensorAll` 位掩码. `setEnabledDevices()` 通过位运算启用或禁用它.

`updateDeviceStatuses()` 对 RS485 发出 `RS485 温湿度计` 状态：采集运行且使能时显示“模拟采集”，暂停时显示“等待采集”，禁用时显示“已禁用”. 当前没有 RS485 串口在线检测，不能从该状态推断总线设备存在.

`sample()` 的实际表达式是：

```cpp
snapshot.humidity = enabledDevices_ & SensorRs485
    ? 54.0 + 8.0 * qSin(t * 0.72 + 0.8) : qQNaN();
```

`sampleIndex_` 每次采样递增，湿度约在 46% 到 62% 之间变化. RS485 温度目前没有独立读数，温度字段由 BMP580 负责.

## 3. 未来真实 Modbus RTU 链路

接入真实模块时，应保持 `ISensorProvider` 边界不变，在采样层增加独立 RS485 provider 或串口 worker：

```text
UART3 打开与波特率配置
  -> Modbus 请求帧（地址、功能码、寄存器、CRC）
  -> 等待响应和超时
  -> CRC、地址、功能码和寄存器范围校验
  -> 原始值缩放为温度/湿度
  -> SensorSnapshot
  -> 界面、告警和 ESP8266 遥测
```

失败时应区分“串口打不开”“响应超时”“CRC 错误”和“寄存器值无效”，而不是继续保留模拟值. 替换模拟表达式时，禁用设备仍应输出 `NaN`，避免旧数据被误用.

## 4. 与其他模块的关系

RS485 不直接参与 ESP8266 串口通信. ESP8266 使用规划中的 UART4，负责网络连接和数据上报；RS485 使用 UART3，负责现场总线. 两者最终只在 `SensorSnapshot` 和主窗口设备状态层汇合.

## 5. 调试建议

当前实现可验证设备复选框、湿度曲线、阈值告警、CSV 的 `rs485_humidity` 列以及 ESP8266 遥测字段. 真实硬件接入前应补充串口回环、Modbus 帧、超时和断线恢复测试，并确认设备树中的 UART3 引脚复用、方向控制和波特率.

## 6. `sensorprovider.cpp` 中 RS485 相关代码逐行解析

```cpp
enum SensorDevice {
    SensorBmp580 = 1,
    SensorRs485 = 2,
    SensorVeml7700 = 4,
    SensorAll = SensorBmp580 | SensorRs485 | SensorVeml7700
};
```

- 每个设备使用一个二进制位, 便于一个整数同时保存多个使能状态. `SensorRs485 = 2` 对应二进制 `010`. SensorAll` 使用按位或得到 `111`, 表示三个设备全部启用.

```cpp
const auto update = [this, collecting](const QString &device, int flag) {
    const bool enabled = enabledDevices_ & flag;
    emit deviceStatusChanged(device, enabled && collecting,
        enabled ? (collecting ? QStringLiteral("模拟采集") : QStringLiteral("等待采集"))
                : QStringLiteral("已禁用"));
};
update(QStringLiteral("RS485 温湿度计"), SensorRs485);
```

- lambda 捕获当前对象和采集定时器状态.
- 按位与判断 RS485 是否被启用.
- `connected` 信号参数只有在“启用且正在采集”时为 true.
- 当前实现把状态详情明确写成“模拟采集”, 不能当作真实串口在线证明.
- 最后一行把 RS485 的显示名称和位标志交给统一状态函数.

```cpp
const double t = sampleIndex_++ / 10.0;
SensorSnapshot snapshot;
snapshot.timestamp = QDateTime::currentDateTime();
...
snapshot.humidity = enabledDevices_ & SensorRs485
    ? 54.0 + 8.0 * qSin(t * 0.72 + 0.8) : qQNaN();
emit snapshotReady(snapshot);
```

- 后缀递增运算先使用旧值计算 `t`, 再让下一次采样索引加 1.
- 除以 10 只是把整数索引映射为较慢变化的模拟时间.
- `SensorSnapshot` 创建本次采样的统一数据容器.
- 时间戳记录采样发生时间.
- 位与判断 RS485 是否启用.
- 启用时计算确定性正弦波, 中心值 54.0, 振幅 8.0, 用于稳定测试曲线.
- 未启用时写入 `NaN`, 下游不会把禁用状态当成有效湿度.
- `snapshotReady` 把快照广播给主窗口和其他订阅者.

## 7. 真实驱动替换位置

当前没有 `QSerialPort`, `termios`, Modbus 地址或 CRC 代码, 所以不能逐行解释不存在的 RS485 驱动. 将来真实实现应把模拟表达式替换为“发送请求, 等待响应, 校验 CRC, 转换寄存器”的独立函数, 并保留本节的位掩码和 `SensorSnapshot` 接口.

## 8. Linux 内核和 RS485

内核树中没有“温湿度计”或 Modbus 专用驱动。i.MX6ULL 只提供通用 UART 驱动, 设备树把 UART 暴露成 `/dev/ttymxcX`, 应用自己决定发送什么协议。

设备树公共定义在 `arch/arm/boot/dts/imx6ull.dtsi`, 其中 `uart3` 对应 `serial@021ec000`, `uart4` 对应 `serial@021f0000`。UART 驱动负责时钟、FIFO、中断和收发字节, 不知道 Modbus 地址、寄存器或温湿度单位。

所以当前代码链路是:

```text
UART3 内核驱动
  -> /dev/ttymxcX
  -> 未来的 Modbus 用户态代码
  -> SensorSnapshot.humidity
```

本项目现在没有打开 UART3 读取 RS485, `sample()` 中的湿度仍然是模拟正弦值。要做真实驱动, 需要在用户态增加串口配置、方向控制、Modbus CRC 和超时处理, 或另外编写内核协议驱动。
