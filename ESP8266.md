# ESP8266 控制器与通信链路

## 1. 模块定位

`Esp8266Controller` 是 Qt 应用与 ESP8266 WiFi 模块之间的异步控制层. 它通过 UART 发送 AT 指令，完成串口生命周期、WiFi 扫描与连接、HTTP 遥测、远程命令轮询、应用 OTA 和系统 OTA.

ESP8266 使用规划中的 UART4，默认波特率为 115200. 控制器通过 Qt signals 把连接状态、扫描结果、OTA 进度和远程配置交给 `WifiDialog`、`MainWindow` 或采样服务.

## 2. 串口生命周期

`availablePorts()` 扫描 `/dev` 下的 `ttymxc*`、`ttyUSB*`、`ttyACM*` 和 `ttyS*`，去重后返回候选路径.

`openPort()` 先调用 `closePort()` 清理旧资源. 空路径进入模拟模式；真实路径使用非阻塞方式打开，设置 raw 模式和波特率，创建 `QSocketNotifier` 监听可读事件，并启动 WiFi 状态和远程命令定时器.

`readAvailable()` 使用非阻塞 `read()` 追加 `receiveBuffer_`，按状态识别 `>`、`+IPD,<length>:` 和换行分隔的 AT 响应. `closePort()` 停止 notifier 和定时器，关闭描述符并释放 OTA 文件.

## 3. 操作状态机

状态枚举把 `Scanning`、`Connecting`、`HttpSending`、`OtaWaitingPrompt` 和 `OtaReceiving` 等互斥操作分开. `sendCommand()` 统一负责写串口和启动一次性超时；超时由 `timeout()` 转换为失败信号或结束当前操作.

操作冲突时使用 `scanQueued_`、`connectQueued_`、`statusQueued_`、`commandQueued_` 和遥测队列延后任务. `scheduleNextTask()` 在当前 TCP 操作完成后继续处理.

## 4. WiFi、遥测和远程命令

`scanNetworks()` 发送 `AT+CWMODE=1` 和 `AT+CWLAP`，`processLine()` 解析网络名、RSSI 和加密类型，最终发出 `scanFinished()`.

`connectNetwork()` 保存 SSID 和密码，发送 `AT+CWJAP`，成功后查询 IP，并通过 `connectionStateChanged()` 通知界面.

`publishTelemetry(const SensorSnapshot &snapshot)` 将温度、湿度、气压、光照和碰撞告警序列化为 JSON，构造 HTTP POST 请求发送到网关. ESP8266 忙时队列保留最新快照，避免网络阻塞拖垮采样.

`pollRemoteCommand()` 定时访问网关命令接口. `processIpdPayload()` 按 `kind` 分派：`ota` 调用 `startOta()`；`system_update` 准备多文件下载；`set_sampling_interval`、`start_collection`、`pause_collection`、`set_device_enabled` 和 `set_thresholds` 分别发出对应 Qt signal. 响应为 `null` 时表示没有待处理命令.

## 5. 应用 OTA 状态机

`startOta(host, port, manifestPath)` 清理旧状态后进入 `beginOtaConnection()`. OTA 使用普通 TCP，不使用透传：

```text
OtaSettingMode
  -> OtaClosing
  -> OtaConnecting
  -> OtaWaitingPrompt
  -> OtaReceiving
```

`sendOtaRequest()` 先请求 manifest，再请求二进制路径. 收到 `>` 后写入 HTTP 请求；收到 `+IPD` 后按声明长度取 payload.

`processHttpData()` 首次解析 HTTP 状态行和 `Content-Length`，之后将 body 写入 manifest 缓冲区或临时文件. 每个数据块刷新空闲超时并发出 `otaProgress()`.

`finishHttpResponse()` 在文件完成后检查大小和 SHA256. 成功发出 `otaPackageReady()`，由 `WifiDialog` 执行替换脚本；失败删除临时文件并调用 `finishWithError()`.

## 6. 系统 OTA

系统 OTA 复用应用 OTA 的 HTTP 接收逻辑，但每个文件保存到 `/mnt/boot/update/<artifact>`. 收到 system manifest 后，`startNextSystemArtifact()` 逐个下载 artifact，校验成功后把摘要追加到 `/mnt/boot/update/manifest.sha256`. 当前没有执行分区写入和 bootloader 切换.

## 7. 调试重点

```bash
ls -l /dev/ttymxc* /dev/ttyUSB* /dev/ttyACM* 2>/dev/null
stty -F /dev/ttymxc3 115200 raw -echo
tail -f /tmp/environment_monitor_ota_progress.log
```

没有 `>` 时检查 `AT+CIPSEND` 长度和连接状态；`+IPD` 长度错误时检查串口分包和波特率；HTTP 没有 `Content-Length` 时当前实现会拒绝响应；OTA 卡住时检查 HTTP server 是否持续发送以及每个数据块是否刷新 idle timeout；远程命令不执行时检查网关命令接口、设备 ID、JSON `kind` 和当前状态机是否被长期操作占用.

## 8. 相关文件

- [控制器头文件](esp8266controller.h)
- [控制器实现](esp8266controller.cpp)
- [WiFi 对话框](wifidialog.cpp)
- [主窗口信号连接](mainwindow.cpp)

## 9. `esp8266controller.cpp` 逐行解析

### 9.1 串口速度和构造函数

```cpp
namespace {
speed_t serialSpeed(int baud)
{
    if (baud == 9600) return B9600;
    if (baud == 57600) return B57600;
    return B115200;
}
}
```

- 匿名命名空间限制辅助函数只在当前源文件可见.
- 输入整数波特率被转换为 Linux `termios` 常量.
- 只显式支持 9600 和 57600, 其他值统一回退 115200.

```cpp
Esp8266Controller::Esp8266Controller(QObject *parent)
    : QObject(parent), fd_(-1), notifier_(nullptr), timeoutTimer_(new QTimer(this)),
      statusTimer_(new QTimer(this)), commandTimer_(new QTimer(this)),
      operation_(Idle), simulated_(false), otaPromptHandled_(false),
      otaHeadersParsed_(false), otaPort_(80), otaExpectedBytes_(0),
      otaExpectedFileBytes_(0), otaReceivedBytes_(0), otaFile_(nullptr),
      otaDownloadingFile_(false), mqttReady_(false), mqttPort_(1883),
      mqttDeviceId_(QStringLiteral("gateway-001"))
{
    timeoutTimer_->setSingleShot(true);
    connect(timeoutTimer_, &QTimer::timeout, this, &Esp8266Controller::timeout);
    statusTimer_->setInterval(15000);
    connect(statusTimer_, &QTimer::timeout, this, &Esp8266Controller::pollWifiStatus);
    commandTimer_->setInterval(3000);
    connect(commandTimer_, &QTimer::timeout, this, &Esp8266Controller::pollRemoteCommand);
}
```

- `fd_ = -1` 表示当前没有打开 Unix 串口. 三个 `QTimer` 分别负责单次超时, 15 秒 WiFi 状态刷新和 3 秒命令轮询. `operation_ = Idle` 让对象从空闲状态开始.
- `mqttHost_` 等网络字段用于构造网关 HTTP 地址.
- `setSingleShot(true)` 使超时定时器触发一次后自动停止.
- `connect` 建立“定时器超时 -> 成员函数”的 Qt 信号链.

### 9.2 打开串口

```cpp
bool Esp8266Controller::openPort(const QString &path, int baudRate)
{
    closePort();
    if (path.isEmpty()) {
        simulated_ = true;
        emit portStateChanged(true, QStringLiteral("ESP8266 模拟模式"));
        return true;
    }
    fd_ = ::open(path.toLocal8Bit().constData(), O_RDWR | O_NOCTTY | O_NONBLOCK);
    if (fd_ < 0) {
        emit operationFailed(QStringLiteral("无法打开串口 %1").arg(path));
        return false;
    }
    termios options{};
    if (tcgetattr(fd_, &options) != 0) {
        closePort();
        emit operationFailed(QStringLiteral("读取串口参数失败"));
        return false;
    }
    cfmakeraw(&options);
    const speed_t speed = serialSpeed(baudRate);
    cfsetispeed(&options, speed);
    cfsetospeed(&options, speed);
    tcsetattr(fd_, TCSANOW, &options);
}
```

- 先关闭旧串口, 保证不会同时保留两个描述符. 空路径选择模拟模式, 用于没有硬件时测试 UI.
- `O_NOCTTY` 防止串口成为进程控制终端, `O_NONBLOCK` 防止读取阻塞 Qt 事件循环.

### 9.3 接收缓存和 `+IPD`

```cpp
while ((size = ::read(fd_, data, sizeof(data))) > 0)
    receiveBuffer_.append(data, static_cast<int>(size));
```

`read()` 一次返回的是当前可取得的字节数, 不是完整 AT 行或完整 TCP 包. 代码先把所有片段追加到持久缓存, 再根据协议边界解析.

```cpp
if ((operation_ == OtaReceiving || operation_ == CommandSending || operation_ == HttpSending)
    && receiveBuffer_.startsWith("+IPD,")) {
    const int colon = receiveBuffer_.indexOf(':');
    if (colon < 0) break;
    const int comma = receiveBuffer_.lastIndexOf(',');
    bool ok = false;
    const int length = receiveBuffer_.mid(comma + 1, colon - comma - 1).toInt(&ok);
    if (!ok || length < 0) { finishWithError(QStringLiteral("ESP8266 +IPD 长度解析失败")); return; }
    if (receiveBuffer_.size() < colon + 1 + length) break;
    const QByteArray payload = receiveBuffer_.mid(colon + 1, length);
    receiveBuffer_.remove(0, colon + 1 + length);
    processIpdPayload(payload);
}
```

- 只有 HTTP 相关状态才把 `+IPD` 当作网络帧.
- 找不到冒号说明帧头还没收完整, 保留缓存等待下一次可读事件.
- 最后一个逗号之后的数字是 payload 长度.
- `toInt(&ok)` 校验长度字段格式.
- payload 未收齐时不能提前解析, 否则会把下一帧数据错位.
- `mid` 取出恰好长度的 body, `remove` 删除已消费字节, 最后交给统一分派函数.

### 9.4 OTA HTTP 头和文件体

```cpp
if (!otaHeadersParsed_) {
    otaHttpHeaders_.append(data);
    const int separator = otaHttpHeaders_.indexOf("\\r\\n\\r\\n");
    if (separator < 0) return;
    const QByteArray headers = otaHttpHeaders_.left(separator);
    const QByteArray body = otaHttpHeaders_.mid(separator + 4);
    otaHttpHeaders_ = headers;
    otaHeadersParsed_ = true;
}
```

这段代码先缓存跨多个 `+IPD` 帧到达的 HTTP 头, 找到空行才认为头结束. 空行前是状态行和 headers, 空行后已经到达的字节属于 body, 不能丢弃.

```cpp
const QRegularExpression expression(QStringLiteral("(?im)^Content-Length:\\s*(\\d+)"));
const QRegularExpressionMatch match = expression.match(QString::fromLatin1(headers));
if (!match.hasMatch()) { finishWithError(QStringLiteral("OTA 响应缺少 Content-Length")); return; }
otaExpectedBytes_ = match.captured(1).toLongLong();
if (otaDownloadingFile_) {
    otaFile_ = new QFile(systemUpdateActive_ ? otaLocalPath_ : QStringLiteral("/tmp/environment_monitor.new"));
    otaFile_->open(QIODevice::WriteOnly | QIODevice::Truncate);
}
```

正则从 HTTP headers 读取 `Content-Length`, 转成期望字节数. 下载文件阶段创建临时文件并截断旧内容, manifest 阶段则继续累积内存 body.

### 9.5 下载完成和 SHA256

```cpp
if (otaDownloadingFile_) {
    otaFile_->flush();
    otaFile_->close();
    delete otaFile_;
    otaFile_ = nullptr;
    QFile file(systemUpdateActive_ ? otaLocalPath_ : QStringLiteral("/tmp/environment_monitor.new"));
    if (file.size() != otaExpectedFileBytes_) { QFile::remove(file.fileName()); finishWithError(...); return; }
    const QString hash = QString::fromLatin1(
        QCryptographicHash::hash(file.readAll(), QCryptographicHash::Sha256).toHex());
    if (hash.compare(otaFileSha256_, Qt::CaseInsensitive) != 0) {
        QFile::remove(file.fileName());
        finishWithError(QStringLiteral("OTA SHA256 校验失败"));
        return;
    }
}
```

- `flush` 把 Qt 缓冲内容写入文件.
- `QCryptographicHash::Sha256` 对完整文件计算摘要并转为十六进制字符串.
- 应用 OTA 校验成功后发出 `otaPackageReady`, 系统 OTA 则追加 `manifest.sha256` 并调度下一个 artifact.

## 9. Linux 内核和 ESP8266

ESP8266 不是 Linux 内核里的传感器驱动。内核只负责 i.MX6ULL 的通用 UART, 设备树把 UART4 暴露成串口设备, Qt 的 `Esp8266Controller` 打开这个设备并发送 AT 指令。

设备树使用 `pinctrl_uart4_wifi` 和 `status = "okay"` 启用 UART4。内核 UART 驱动负责把硬件收发变成 `/dev/ttymxcX`, 但不会理解 `AT+CWMODE`, `AT+CIPSTART` 或 `+IPD`。

真正的链路是:

```text
UART4 内核驱动
  -> /dev/ttymxcX
  -> Esp8266Controller::openPort()
  -> AT 指令和 +IPD 数据
  -> WiFi, HTTP, 遥测和 OTA
```

因此 ESP8266 的“驱动代码”主要是项目里的 `esp8266controller.cpp`, Linux 目录中对应的是通用 `fsl-imx` UART 驱动, 没有 ESP8266 专用 `.c` 文件。排查时应分别看 `dmesg` 的 UART 注册, `/dev/ttymxc*` 是否存在, 以及 Qt 的 AT 日志。
