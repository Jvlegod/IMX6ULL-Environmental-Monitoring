# BMP580 驱动与数据链路

## 1. 模块定位

BMP580 是本项目的温度和气压传感器. 硬件通过 Linux 内核 IIO（Industrial I/O）框架暴露为 sysfs 设备，Qt 应用不直接操作 SPI 寄存器，而是读取内核已经转换好的输入文件.

本项目负责发现名为 `bmp580` 的 IIO 设备，读取温度和压力，将结果写入 `SensorSnapshot`，再交给主窗口显示、告警和 ESP8266 遥测发送.

## 2. 设备节点

默认扫描目录为 `/sys/bus/iio/devices/iio:device*`，需要存在：

```text
name                 -> bmp580
in_temp_input        -> 温度值
in_pressure_input    -> 气压值
```

代码按内核 IIO 节点输出读取，不在用户态重新实现 BMP580 补偿算法. 具体缩放规则由内核驱动和设备树配置决定.

## 3. 调用链

```text
MainWindow 启动采集
  -> ISensorProvider::start
  -> QTimer::timeout
  -> SimulatedSensorProvider::sample
  -> discoverBmp580Path / readBmp580
  -> SensorSnapshot.temperature / pressure
  -> snapshotReady
  -> MainWindow 更新曲线、数值和告警
  -> Esp8266Controller::publishTelemetry
```

设备状态通过 `deviceStatusChanged("BMP580 / I2C", connected, detail)` 传递，状态包括“等待采集”“IIO 不可用”“IIO 采集”和“IIO 读取失败”.

## 4. 调试和限制

```bash
find /sys/bus/iio/devices -maxdepth 2 -name name -exec sh -c 'printf "%s: " "$1"; cat "$1"' _ {} \\\;
cat /sys/bus/iio/devices/iio:deviceX/in_temp_input
cat /sys/bus/iio/devices/iio:deviceX/in_pressure_input
```

无真实硬件时可使用：

```bash
export ENVIRONMENT_MONITOR_IIO_ROOT=/tmp/test-iio
export ENVIRONMENT_MONITOR_BMP580_SYSFS=/tmp/test-iio/iio:device0
```

当前代码没有在 Qt 层实现 SPI 初始化、校准参数配置或采样触发，这些属于 Linux 内核 IIO 驱动. 若单位不符合预期，应先核对设备树和内核 IIO scale.

## 5. `sensorprovider.cpp` 逐行解析

下面按源码执行顺序解释 BMP580 相关语句. 空行和只包含花括号的行不重复说明, 但每条有运行效果的语句都对应一个解释.

### 5.1 构造函数和初始状态

```cpp
SimulatedSensorProvider::SimulatedSensorProvider(QObject *parent)
    : ISensorProvider(parent), timer_(new QTimer(this)), sampleIndex_(0), enabledDevices_(SensorAll),
      bmp580Path_(discoverBmp580Path()), bmp580Online_(!bmp580Path_.isEmpty()),
      veml7700Path_(discoverVeml7700Path()), veml7700Online_(!veml7700Path_.isEmpty())
{
    timer_->setInterval(1000);
    connect(timer_, &QTimer::timeout, this, &SimulatedSensorProvider::sample);
}
```

- 函数签名接收 Qt 父对象, 让 QObject 自动管理生命周期.
- `ISensorProvider(parent)` 先初始化抽象采集接口的 QObject 基类.
- `new QTimer(this)` 创建采样定时器, 父对象是当前 provider, 因此析构时自动释放.
- `sampleIndex_(0)` 把模拟时间从第 0 个样本开始.
- `enabledDevices_(SensorAll)` 默认打开 BMP580, RS485 和 VEML7700 三个位.
- `bmp580Path_(discoverBmp580Path())` 在对象构造阶段寻找真实 IIO 目录.
- `bmp580Online_(!bmp580Path_.isEmpty())` 只把“找到设备目录”作为初始在线条件, 真正可读性仍由第一次采样验证.
- `timer_->setInterval(1000)` 将默认周期设置为 1000 ms.
- `connect(...)` 把定时器超时信号连接到 `sample()`, 之后每次超时都会执行一次采样.

### 5.2 `sample()` 中的 BMP580 分支

```cpp
if (enabledDevices_ & SensorBmp580) {
    if (bmp580Path_.isEmpty())
        bmp580Path_ = discoverBmp580Path();

    QString errorMessage;
    if (bmp580Path_.isEmpty() || !readBmp580(&snapshot.temperature,
                                              &snapshot.pressure,
                                              &errorMessage)) {
        snapshot.temperature = qQNaN();
        snapshot.pressure = qQNaN();
        if (bmp580Online_) {
            bmp580Online_ = false;
            if (!errorMessage.isEmpty())
                emit providerError(QStringLiteral("BMP580 IIO 读取失败: %1").arg(errorMessage));
            updateDeviceStatuses();
        }
    } else if (!bmp580Online_) {
        bmp580Online_ = true;
        updateDeviceStatuses();
    }
}
```

- 位与表达式 `enabledDevices_ & SensorBmp580` 检查 BMP580 是否启用.
- 路径为空时重新扫描, 支持设备稍后由内核注册的情况.
- `errorMessage` 用来把底层文件错误传回状态层.
- `readBmp580()` 接收两个输出指针, 直接写入当前快照的温度和气压字段.
- 路径为空或读取返回 false 都进入失败分支.
- `qQNaN()` 明确表示本次数据不可用, 下游可以用 `qIsFinite()` 过滤.
- 只有从在线变为离线时才发错误和刷新状态, 防止每秒重复刷屏.
- 读取恢复后把 `bmp580Online_` 置回 true, 并刷新设备状态.

### 5.3 `discoverBmp580Path()` 逐行含义

```cpp
const QString configuredPath = qEnvironmentVariable("ENVIRONMENT_MONITOR_BMP580_SYSFS").trimmed();
if (!configuredPath.isEmpty())
    return QFile::exists(configuredPath) ? configuredPath : QString();
const QString iioRoot = qEnvironmentVariable("ENVIRONMENT_MONITOR_IIO_ROOT",
                                              QStringLiteral("/sys/bus/iio/devices"));
const QDir root(iioRoot);
const QStringList devices = root.entryList(QStringList() << QStringLiteral("iio:device*"),
                                            QDir::Dirs | QDir::NoDotAndDotDot, QDir::Name);
for (const QString &device : devices) {
    QFile nameFile(root.filePath(device + QStringLiteral("/name")));
    if (!nameFile.open(QIODevice::ReadOnly | QIODevice::Text))
        continue;
    if (QString::fromLocal8Bit(nameFile.readAll()).trimmed().compare(
            QStringLiteral("bmp580"), Qt::CaseInsensitive) == 0)
        return root.filePath(device);
}
return QString();
```

- 环境变量允许测试目录覆盖真实 `/sys` 路径.
- 覆盖路径非空时只接受存在的路径, 不再继续扫描默认目录.
- `ENVIRONMENT_MONITOR_IIO_ROOT` 未设置时使用内核 IIO 标准目录.
- `QDir` 封装目录操作, `entryList` 只取目录, 排除 `.` 和 `..`, 并按名称排序.
- 循环逐个打开设备的 `name` 文件, 文件打不开就跳过当前设备.
- `readAll()` 读取设备名, `trimmed()` 去掉内核文件末尾换行.
- `compare(..., Qt::CaseInsensitive)` 允许 `bmp580` 大小写不同.
- 返回目录而不是 `name` 文件路径, 因为读取函数稍后会在目录后拼接输入文件名.
- 循环结束仍未匹配时返回空字符串, 由调用者解释为设备不存在.

### 5.4 `readBmp580()` 逐行含义

```cpp
const auto readValue = [errorMessage](const QString &path, double *value) {
    QFile inputFile(path);
    if (!inputFile.open(QIODevice::ReadOnly | QIODevice::Text)) {
        if (errorMessage) *errorMessage = inputFile.errorString();
        return false;
    }
    bool ok = false;
    *value = QString::fromLocal8Bit(inputFile.readAll()).trimmed().toDouble(&ok);
    if (!ok || !qIsFinite(*value)) {
        if (errorMessage) *errorMessage = QStringLiteral("BMP580 数值无效");
        return false;
    }
    return true;
};
if (!readValue(bmp580Path_ + QStringLiteral("/in_temp_input"), temperature))
    return false;
if (!readValue(bmp580Path_ + QStringLiteral("/in_pressure_input"), pressure))
    return false;
return true;
```

- lambda 把“打开、读取、转换、校验”四步复用到温度和气压.
- 按值捕获 `errorMessage` 指针, 所以可以修改调用者提供的错误字符串.
- `QFile` 只读打开 sysfs 文件, 打不开时保存系统错误并返回 false.
- `toDouble(&ok)` 同时完成文本到浮点数转换和格式合法性检查.
- `qIsFinite` 拒绝 NaN 和无穷大, 防止异常值进入曲线和告警计算.
- 先读温度, 再读压力, 任一个失败都会短路返回.
- 两个文件都成功时返回 true, 此时输出指针已经写入有效读数.

## 7. Linux 内核里的 BMP580

真正和 BMP580 芯片通信的代码在内核目录:

- `drivers/iio/pressure/bmp580-i2c.c`

```c
/*
 * I2C driver for the Bosch Sensortec BMP580 pressure sensor.
 *
 * The BMP580 reports internally compensated 24-bit temperature and
 * pressure values. Temperature is encoded in degrees Celsius divided by
 * 65536 and pressure in pascals divided by 64.
 */

#define pr_fmt(fmt) "bmp580: " fmt

#include <linux/delay.h>
#include <linux/module.h>
#include <linux/mutex.h>
#include <linux/of.h>
#include <linux/i2c.h>
#include <linux/iio/iio.h>

#define BMP580_REG_CHIP_ID		0x01
#define BMP580_CHIP_ID			0x50
#define BMP580_REG_STATUS		0x28
#define BMP580_STATUS_NVM_RDY		BIT(1)
#define BMP580_STATUS_NVM_ERR		BIT(2)
#define BMP580_STATUS_NVM_CMD_ERR	BIT(3)
#define BMP580_REG_INT_STATUS		0x27
#define BMP580_INT_STATUS_POR		BIT(4)
#define BMP580_REG_TEMP_XLSB		0x1D
#define BMP580_REG_OSR_CONFIG		0x36
#define BMP580_REG_ODR_CONFIG		0x37
#define BMP580_REG_CMD			0x7E
#define BMP580_CMD_SOFT_RESET		0xB6

#define BMP580_OSR_PRESS_EN		BIT(6)
#define BMP580_OSR_PRESS_4X		(2 << 3)
#define BMP580_OSR_TEMP_1X		0
#define BMP580_ODR_1HZ			(0x1C << 2)	/* 1 Hz */
#define BMP580_MODE_NORMAL			0x01
struct bmp580_data {
	struct i2c_client *client;
	struct mutex lock;
};

static bool bmp580_debug;
static bool bmp580_skip_powerup_dummy;
static bool bmp580_skip_reset_dummy;

module_param_named(debug, bmp580_debug, bool, 0644);
MODULE_PARM_DESC(debug, "Log BMP580 I2C initialization and register reads");
module_param_named(skip_powerup_dummy, bmp580_skip_powerup_dummy, bool, 0644);
MODULE_PARM_DESC(skip_powerup_dummy, "Skip the first I2C read after power-up");
module_param_named(skip_reset_dummy, bmp580_skip_reset_dummy, bool, 0644);
MODULE_PARM_DESC(skip_reset_dummy, "Skip the first I2C read after soft reset");

static const struct iio_chan_spec bmp580_channels[] = {
	{
		.type = IIO_PRESSURE,
		.info_mask_separate = BIT(IIO_CHAN_INFO_PROCESSED),
	},
	{
		.type = IIO_TEMP,
		.info_mask_separate = BIT(IIO_CHAN_INFO_PROCESSED),
	},
};

static int bmp580_read(struct i2c_client *client, u8 reg, void *buf, size_t len)
{
	int ret = i2c_smbus_read_i2c_block_data(client, reg, len, buf);

	return ret < 0 ? ret : 0;
}

static int bmp580_write(struct i2c_client *client, u8 reg, u8 value)
{
	return i2c_smbus_write_byte_data(client, reg, value);
}

static int bmp580_read_raw(struct iio_dev *indio_dev,
				   struct iio_chan_spec const *chan,
				   int *val, int *val2, long mask)
{
	struct bmp580_data *data = iio_priv(indio_dev);
	u8 raw[6];
	u32 pressure;
	s32 temperature;
	int ret;

	if (mask != IIO_CHAN_INFO_PROCESSED)
		return -EINVAL;

	mutex_lock(&data->lock);
	ret = bmp580_read(data->client, BMP580_REG_TEMP_XLSB, raw, sizeof(raw));
	if (ret)
		goto unlock;

	temperature = raw[0] | ((s32)raw[1] << 8) | ((s32)raw[2] << 16);
	if (temperature & BIT(23))
		temperature |= ~0x00ffffff;
	pressure = raw[3] | ((u32)raw[4] << 8) | ((u32)raw[5] << 16);

	if (chan->type == IIO_TEMP) {
		*val = temperature;
		*val2 = 65536;
		ret = IIO_VAL_FRACTIONAL;
	} else if (chan->type == IIO_PRESSURE) {
		*val = pressure;
		*val2 = 64000;
		ret = IIO_VAL_FRACTIONAL;
	} else {
		ret = -EINVAL;
	}

unlock:
	mutex_unlock(&data->lock);
	return ret;
}

static const struct iio_info bmp580_info = {
	.driver_module = THIS_MODULE,
	.read_raw = bmp580_read_raw,
};

static int bmp580_init(struct bmp580_data *data)
{
	u8 value;
	int ret;

	msleep(5);
	if (!bmp580_skip_powerup_dummy) {
		/* The first SPI access after power-up is a dummy read. */
		ret = bmp580_read(data->client, BMP580_REG_CHIP_ID, &value, 1);
		if (ret)
			return ret;
		if (bmp580_debug)
			dev_info(&data->client->dev, "power-up dummy read discarded value=0x%02x\n", value);
	} else if (bmp580_debug) {
		dev_info(&data->client->dev, "power-up dummy read skipped\n");
	}

	ret = bmp580_write(data->client, BMP580_REG_CMD, BMP580_CMD_SOFT_RESET);
	if (ret)
		return ret;
	msleep(5);

	if (!bmp580_skip_reset_dummy) {
		/* The first SPI access after reset is a dummy read. */
		ret = bmp580_read(data->client, BMP580_REG_CHIP_ID, &value, 1);
		if (ret)
			return ret;
		if (bmp580_debug)
			dev_info(&data->client->dev, "reset dummy read discarded value=0x%02x\n", value);
	} else if (bmp580_debug) {
		dev_info(&data->client->dev, "reset dummy read skipped\n");
	}

	ret = bmp580_read(data->client, BMP580_REG_CHIP_ID, &value, 1);
	if (ret)
		return ret;
	if (bmp580_debug)
		dev_info(&data->client->dev, "chip ID read value=0x%02x expected=0x%02x\n",
			value, BMP580_CHIP_ID);
	if (value != BMP580_CHIP_ID) {
		dev_err(&data->client->dev,
			"unexpected chip ID 0x%02x, expected 0x%02x\n",
			value, BMP580_CHIP_ID);
		return -ENODEV;
	}

	ret = bmp580_read(data->client, BMP580_REG_STATUS, &value, 1);
	if (ret)
		return ret;
	if (bmp580_debug)
		dev_info(&data->client->dev, "status=0x%02x\n", value);
	if (!(value & BMP580_STATUS_NVM_RDY) ||
	    (value & (BMP580_STATUS_NVM_ERR | BMP580_STATUS_NVM_CMD_ERR)))
		return -EIO;

	ret = bmp580_read(data->client, BMP580_REG_INT_STATUS, &value, 1);
	if (ret)
		return ret;
	if (bmp580_debug)
		dev_info(&data->client->dev, "int_status=0x%02x\n", value);
	if (!(value & BMP580_INT_STATUS_POR))
		return -EIO;

	ret = bmp580_write(data->client, BMP580_REG_OSR_CONFIG,
				   BMP580_OSR_PRESS_EN | BMP580_OSR_PRESS_4X |
				   BMP580_OSR_TEMP_1X);
	if (ret)
		return ret;

	return bmp580_write(data->client, BMP580_REG_ODR_CONFIG,
				    BMP580_ODR_1HZ | BMP580_MODE_NORMAL);
}

static int bmp580_probe(struct i2c_client *client, const struct i2c_device_id *id)
{
	struct iio_dev *indio_dev;
	struct bmp580_data *data;
	int ret;

	indio_dev = devm_iio_device_alloc(&client->dev, sizeof(*data));
	if (!indio_dev)
		return -ENOMEM;

	data = iio_priv(indio_dev);
	data->client = client;
	mutex_init(&data->lock);
	indio_dev->dev.parent = &client->dev;
	indio_dev->name = "bmp580";
	indio_dev->channels = bmp580_channels;
	indio_dev->num_channels = ARRAY_SIZE(bmp580_channels);
	indio_dev->info = &bmp580_info;
	indio_dev->modes = INDIO_DIRECT_MODE;

	ret = bmp580_init(data);
	if (ret) {
		dev_err(&client->dev, "initialization failed: %d\n", ret);
		return ret;
	}

	return devm_iio_device_register(&client->dev, indio_dev);
}

static const struct i2c_device_id bmp580_i2c_id[] = {
	{ "bmp580", 0 },
	{ },
};
MODULE_DEVICE_TABLE(i2c, bmp580_i2c_id);

static const struct of_device_id bmp580_of_match[] = {
	{ .compatible = "bosch,bmp580" },
	{ },
};
MODULE_DEVICE_TABLE(of, bmp580_of_match);

static struct i2c_driver bmp580_driver = {
	.driver = {
		.name = "bmp580-i2c",
		.of_match_table = of_match_ptr(bmp580_of_match),
	},
	.probe = bmp580_probe,
	.id_table = bmp580_i2c_id,
};
module_i2c_driver(bmp580_driver);

MODULE_AUTHOR("Keke Ming <ming.jvle@gmail.com>");
MODULE_DESCRIPTION("I2C driver for Bosch Sensortec BMP580");
MODULE_LICENSE("GPL v2");
```

这两个文件做的是同一件事, 区别只是一个走 SPI, 一个走 I2C. 当前 EVK 设备树使用 I2C 版本. 

驱动开头定义寄存器地址, 例如 `0x01` 是芯片 ID, `0x1D` 是温度和压力数据的起点, `0x7E` 是命令寄存器. `BMP580_CHIP_ID` 写成 `0x50`, probe 时读到别的值就说明总线上不是 BMP580. 

```c
struct bmp580_data {
    struct i2c_client *client;  /* SPI 文件中是 spi_device */
    struct mutex lock;
};
```

这个结构体就是驱动保存 I2C/SPI 设备对象和一把锁. 锁的作用是同一时刻只允许一个读取操作访问芯片. 

读取数据时, 驱动从 `0x1D` 连续读 6 个字节. 前 3 个字节拼成温度, 后 3 个字节拼成压力:

```c
temperature = raw[0] | ((s32)raw[1] << 8) | ((s32)raw[2] << 16);
pressure = raw[3] | ((u32)raw[4] << 8) | ((u32)raw[5] << 16);
```

BMP580 的温度是 24 位有符号数, 所以下面代码检查最高位并补齐负数的高位:

```c
if (temperature & BIT(23))
    temperature |= ~0x00ffffff;
```

驱动不会直接返回浮点数. 它把温度写成“整数 / 65536”, 压力写成“整数 / 64000”, 再交给 IIO 框架:

```c
*val = temperature;
*val2 = 65536;
return IIO_VAL_FRACTIONAL;
```

Qt 最后只需要读取 `in_temp_input` 和 `in_pressure_input`, 不需要自己读 SPI 寄存器或做 BMP580 补偿. 

初始化时, `bmp580_init()` 先等待 5 ms, 发送软件复位 `0xB6`, 再检查芯片 ID, NVM 状态和上电状态. 检查失败就返回错误, 设备不会注册成功. 检查通过后, 驱动把压力过采样设为 4 倍, 温度过采样设为 1 倍, 输出频率设为 1 Hz, 然后进入正常模式. 

`bmp580_probe()` 是内核发现设备后的入口. 它设置 SPI 的 8 位字长、Mode 0 和最高 10 MHz 频率（I2C 版本没有这些设置）, 分配 IIO 设备, 填入 `read_raw` 回调, 最后调用 `devm_iio_device_register()`. 注册成功后, 内核才会创建 `iio:deviceX` 和输入属性. 

设备树中实际绑定是:

```dts
&i2c2 {
    status = "okay";
    bmp580@46 {
        compatible = "bosch,bmp580";
        reg = <0x47>;
    };
};
```

`compatible` 要和驱动里的匹配表相同, `reg` 才是实际 I2C 地址. 这里节点名写成 `bmp580@46`, 但地址是 `0x47`, 以 `reg` 为准. 驱动注册出的 `name` 是 `bmp580`, 正好被 Qt 的 `discoverBmp580Path()` 找到. 

因此完整链路很简单:

```text
设备树 compatible
  -> bmp580-i2c.c 的 probe
  -> IIO 注册
  -> /sys/bus/iio/devices/iio:deviceX/in_temp_input
  -> Qt readBmp580()
  -> SensorSnapshot
```
